# simple-hbd

Minimal Vite + React scaffold for the birthday card project so it can be built and deployed on Vercel.

Quick start:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm run preview
```
